{
  "schema": "triad-review-legs.v2-draft",
  "legs": [
    {
      "name": "claude",
      "vendor": "claude",
      "enabled": true,
      "acceptance": "required",
      "claude": {
        "agent": "cross-family-review-reviewer",
        "model": null,
        "effort": "xhigh"
      },
      "timeout_s": 900,
      "note": "model null = the host's default (A: the agent's frontmatter; B: its claude CLI default); agent applies on A only"
    },
    {
      "name": "codex",
      "vendor": "codex",
      "enabled": true,
      "acceptance": "required",
      "codex": {
        "model": null,
        "reasoning": "xhigh"
      },
      "timeout_s": 900,
      "note": "model null = the host's default codex model"
    },
    {
      "name": "google",
      "vendor": "google",
      "enabled": true,
      "acceptance": "participating",
      "agy": {
        "model": "<dispatch-time slug>",
        "effort": "high"
      },
      "gemini": {
        "model": "<route-valid Pro model id; HIGH comes from its default thinking level>",
        "effort": null
      },
      "timeout_s": 600,
      "note": "vendor google = resolved at dispatch by the host's shipped chain; add `route`: `agy` | `gemini` to pin when both CLIs are present (R-GOOGLE). Default model = Pro family + verifiable HIGH on either CLI (R-NOCOST); `effort` applies to agy only. Timeouts are adapter-validated (B formal gemini 600 s; B claude route 1200 s). Placeholders mark this entry as a TEMPLATE until the hosts' catalogs are recorded"
    },
    {
      "name": "x-claude-high",
      "vendor": "claude",
      "enabled": false,
      "acceptance": "informational",
      "claude": {
        "agent": "cross-family-review-reviewer-high",
        "effort": "high",
        "model": null
      },
      "note": "opt-in example of a second claude arm (enable to run it); acceptance is data only — a verified blocking finding from this leg blocks the round (R-AGREE)",
      "timeout_s": 900
    },
    {
      "name": "trial",
      "vendor": "google",
      "enabled": false,
      "acceptance": "informational",
      "agy": {
        "model": "<slug under test>",
        "effort": "high"
      },
      "note": "opt-in trial model on the Google family (agy block); acceptance is data only (R-AGREE)",
      "timeout_s": 900
    }
  ],
  "_comment": "ILLUSTRATIVE TEMPLATE, not a runnable default: the three enabled legs are the default roster shape; placeholder model values must be replaced from each host's recorded catalog before dispatch. Fields and resolution rules: reference/review-rules.md#R-ROSTER; keys deliberately absent: CHANGELOG.md rev-0"
}
