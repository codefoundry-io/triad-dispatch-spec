{
  "schema": "triad-cases.v1-draft",
  "note": "rule anchors: reference/review-rules.md; unrun handling: reference/spec-authoring.md § 4",
  "cases": [
    {
      "id": "C1",
      "surface": "engine-transport",
      "rule": "R-TERMINAL",
      "summary": "Normal child exit with inherited stdout/stderr; incomplete stdin; timeout; catchable SIGTERM/SIGHUP",
      "input": "a vendor child that exits normally with inherited pipes / one whose stdin write fails / one that exceeds the timeout / one killed by SIGTERM",
      "expected": "Success only after readers joined and the owned group is reaped; incomplete stdin = fail closed (A token input-delivery-failed / exit 65; B RunResult flag); timeout and signals leave a terminal record and no orphan",
      "tests": {
        "A": "tests/unit/wrappers/t48*, tests/integration/f12* (stdin); timeout/signal: todo",
        "B": "Implemented and regression-covered in B e29d820 (P1, triad-b-p1-r2 ADMITTED_SAFE): tests/test_terminal_transport.py and existing stdin/provider tests cover bounded readers/stdin reconciliation, owned-group cleanup, normal exit and timeout/error custody. This is source/test evidence, not shared receipt-schema or every provider/platform conformance."
      }
    },
    {
      "id": "C2",
      "surface": "engine-transport",
      "rule": "R-TERMINAL",
      "summary": "Child or reader-thread start failure after partial setup",
      "input": "a reader-thread or child start that raises after partial setup",
      "expected": "Terminal failure recorded; partial resources released; no success token",
      "tests": {
        "A": "todo",
        "B": "Implemented in B e29d820: tests/test_terminal_transport.py covers post-spawn reader/writer start failures and cleanup of the child and already-started threads; existing spawn/interrupt cases retained. triad-b-p1-r2 ADMITTED_SAFE."
      }
    },
    {
      "id": "C3",
      "surface": "engine-transport",
      "rule": "R-CLEANUP",
      "summary": "Fresh repair-IPC files under the cap-based prune",
      "input": "a run-log dir over its file cap containing a sibling file younger than the age floor",
      "expected": "A file younger than the age floor survives a cap prune; only the time-based sweep may delete by age",
      "tests": {
        "A": "todo (cap path has no floor — verified 2026-09-19)",
        "B": "Existing coverage: tests/test_log_cleanup.py::test_run_log_count_cap_prunes_only_stale_files_and_allows_fresh_overflow and byte-cap counterpart; included in completed macOS/Ubuntu full-suite runs summarized at C24. No installed-provider conformance inferred."
      }
    },
    {
      "id": "C4",
      "surface": "cleanup",
      "rule": "R-CLEANUP",
      "summary": "Foreign directories, false markers, symlinks, dirty or untracked content in the review root",
      "input": "a review root holding (i) a foreign date-prefixed `*.pruning` dir not created by the helper, (ii) a dirty or untracked regular file that is part of the reviewed basis in the ORIGINAL guarded checkout, (iii) the helper's OWN temporary review copy after the agreed evidence-export checks passed",
      "expected": "(i) preserved and reported — cleanup refuses without deleting and states what it observes; (ii) original or foreign content is never disposed of by cleanup; (iii) a provably owned temporary copy may be disposed of, only after the evidence-export checks (R-CLEANUP). Symlinks: C26 (Q4 RULED)",
      "tests": {
        "A": "todo (`.pruning` name-shape branch deletes — verified 2026-09-19)",
        "B": "Allocation/export portion implemented in B a85a800: tests/test_review_cleanup_custody.py covers refusal without provenance, foreign roots, verified export and disposal. Existing UID/type/age and internal-symlink checks retained; C26 separately tracks reviewer link visibility. triad-b-p2-r2 ADMITTED_SAFE."
      }
    },
    {
      "id": "C5",
      "surface": "cleanup",
      "rule": "R-CLEANUP",
      "summary": "Partial deletion resumes only with a verifiable ownership claim",
      "input": "an interrupted cleanup with a partially removed round tree",
      "expected": "Resume requires the allocation record or marker; otherwise refuse",
      "tests": {
        "A": "todo",
        "B": "Implemented in B a85a800: tests/test_review_cleanup_custody.py partial-cleanup resume requires a validated allocation-bound claim. triad-b-p2-r2 ADMITTED_SAFE."
      }
    },
    {
      "id": "C6",
      "surface": "engine-transport",
      "rule": "R-TERMINAL",
      "summary": "Settings-guard release failure after a completed transcript",
      "input": "a completed transcript followed by a settings-guard restore error",
      "expected": "Still a failure; the transcript is preserved",
      "tests": {
        "A": "todo",
        "B": "Implemented in B e29d820: tests/test_antigravity_stream_json.py covers completed transcript followed by settings-guard release failure; terminal failure preserves captured evidence. triad-b-p1-r2 ADMITTED_SAFE."
      }
    },
    {
      "id": "C7",
      "surface": "cleanup",
      "rule": "R-CLEANUP",
      "summary": "Export verified before disposal; cleanup idempotent",
      "input": "a round whose evidence export has not been verified; a second cleanup call",
      "expected": "Second cleanup is a no-op; disposal never precedes export verification",
      "tests": {
        "A": "todo",
        "B": "Implemented in B a85a800: tests/test_review_cleanup_custody.py requires/revalidates an external export before disposal; existing missing-root idempotence remains covered. triad-b-p2-r2 ADMITTED_SAFE."
      }
    },
    {
      "id": "C8",
      "surface": "exit-tokens",
      "rule": "R-TOKENS",
      "summary": "Exit-token membership",
      "input": "every token the host's classifier can emit",
      "expected": "Every emitted token is in contracts/exit-tokens.json with the same code; exceptions listed explicitly (the shipped `is not None` assert is vacuous on both hosts)",
      "tests": {
        "A": "todo",
        "B": "6653bdc: tests/test_exit_token_contract.py checks all 18 shared map rows, classifier/vendor-map membership, bounded producer-literal census and explicit direct/alias exceptions. Three missing map rows added; producer/stdin/repair behavior unchanged. triad-b-wrapper-contracts-r1 ADMITTED_SAFE; one non-blocking local-variable census suggestion retained after leader verified the current token is registered. See spikes/2026-09-20-b-wrapper-contracts.md."
      }
    },
    {
      "id": "C9",
      "surface": "engine-transport",
      "rule": "R-RECEIPT",
      "summary": "Transport receipt fields",
      "input": "one dispatch on each route",
      "expected": "Receipt carries stdin delivery class, route, binary, observed version",
      "tests": {
        "A": "todo",
        "B": "PARTIAL at 8fd5245 (P1): tests/test_common_transport_receipt.py verifies common CLI transport production, actual stdin/route/binary/version and not-started/unexposed cases. Legacy invocation attempt remains 1. Native receipt production and per-entry/attempt v2 collection remain pending. Current status: decisions/host-b-0555-contract-compliance-handoff.md."
      }
    },
    {
      "id": "C10",
      "surface": "engine-transport",
      "rule": "R-RECEIPT, R-BIND",
      "summary": "Audit and run-log field schema",
      "input": "one audit line and one run-log record per host",
      "expected": "Both hosts write the agreed fields; a leg mislabelling its family is caught by binding, not by the audit",
      "tests": {
        "A": "todo",
        "B": "PARTIAL at 8fd5245 (P1): tests/test_common_transport_receipt.py verifies identical common transport data in audit and failure IPC while existing masking/failure-only behavior survives. Native and v2 per-entry admission integration remain pending; audit does not replace result binding."
      }
    },
    {
      "id": "C11",
      "surface": "engine-transport",
      "rule": "R-NOCOST",
      "summary": "Child environment scrub set as data",
      "input": "a leader env carrying loader, credential, endpoint and model-selector variables",
      "expected": "Loader/interpreter injection variables removed on every route; credential/endpoint/model-selector removal per the agreed set (A: no per-call seam today — verified)",
      "tests": {
        "A": "todo",
        "B": "Existing coverage: tests/test_provider_wrappers.py formal env/preflight cases and tests/test_google_diagnostics.py production-scrub fake; preserve shared injection and route-specific scrub sets. No empty-environment requirement."
      }
    },
    {
      "id": "C12",
      "surface": "roster",
      "rule": "R-ROSTER, R-PREPARE",
      "summary": "Roster resolution",
      "input": "no file / a file overriding one default by name / a file with an unknown key / two Google CLIs present",
      "expected": "No file = three standing legs at recommended defaults; file entry overrides by name; unknown key refused; resolved roster printed before dispatch; unselected legs never start; runtime Google resolution recorded",
      "tests": {
        "A": "tests/unit/skills/t7*, t8* (v1 config arms) — v2: todo",
        "B": "PARTIAL at 7cc70a7 (P2): bin/review_roster.py and review_round resolve-roster implement defaults, .agents/triad-review-legs.json discovery, merge-by-name, strict validation and enabled-roster display; tests/test_review_roster.py. Actual prepare/dispatch/capability consumption remains pending; capabilities_checked=false is explicit."
      }
    },
    {
      "id": "C13",
      "surface": "verdict-wire",
      "rule": "R-AGREE",
      "summary": "Agreement and release",
      "input": "legs returning a verdict with no blocking finding, a non-affirmative verdict carrying only Minor findings on unchanged bytes, a blocking finding, an unresolved open question, and no result",
      "expected": "A blocking finding or an unresolved open question from any leg blocks; a Minor-only non-affirmative verdict on the unchanged bytes counts as agreement and is recorded as a verdict-selection deviation; a leg that did not run is not agreement; probe-refuted / owner-decided releases are recorded",
      "tests": {
        "A": "todo",
        "B": "PARTIAL: tests/test_verdict_schema.py::test_safe_accepts_minor_finding and test_not_safe_requires_blocker_or_open_question; Minor-only non-affirmative remains rejected pending wire migration."
      }
    },
    {
      "id": "C14",
      "surface": "verdict-wire",
      "rule": "R-BIND",
      "summary": "Verdict binding",
      "input": "a verdict with a wrong review_id / family / digest / attempt / route; a raw reply with a duplicate member",
      "expected": "review_id + leg identity + attempt + family + resolved route + content digest must match; duplicate JSON members rejected at the original-text boundary (A: only the raw-reply admission rejects today — verified)",
      "tests": {
        "A": "tests/unit/skills/* validate_verdict cases (partial)",
        "B": "PARTIAL: B bc9ff69 and tests/test_verdict_original_json.py reject duplicate original JSON before normalization, including reserved AGY binding completeness; existing ID/family/digest validation retained. triad-b-p3a-r2 ADMITTED_SAFE. leg_name/attempt/route remain v2-dependent."
      }
    },
    {
      "id": "C15",
      "surface": "gemini-policy",
      "rule": "R-CONTAIN",
      "summary": "Gemini host profiles remain byte-identical to their selected contract and deny REVIEW web tools explicitly (D-9, D-B1)",
      "input": "the shipped TOML vs the contract file; a dedicated fixture with read/search controls and attempted web/write calls; final-model prose without engine evidence; a candidate policy with a catch-all and a base-versus-candidate unlisted-tool control",
      "expected": "Each shipped profile is BYTE-IDENTICAL to its selected complete contract: A contracts/gemini-readonly.toml (unchanged 100/200 profile), B contracts/gemini-readonly-b.toml (999 allow/deny plus existing 998 catch-all and Plan Mode transition denies). Both explicitly deny google_web_search and web_fetch. B moves only those tools out of its prior allow set; no overlay or loss of existing protection. Runtime effect requires attributable engine call/result or authoritative policy-exclusion evidence; model prose or missing files alone never prove enforcement. Unknown evidence is INCONCLUSIVE. Preserve local reads/search. A's V1-V5 remain NOT RUN and its proposed rows remain subject to contracts/gemini-readonly.verify.toml. B live checks are separate in contracts/gemini-readonly-b.verify.toml, also NOT RUN. Source/byte tests do not imply runtime conformance.",
      "tests": {
        "A": "tests/unit/wrappers/t50-gemini-policy-web-deny-c15.sh (file shape + byte identity with the contract, offline); runtime checks V1-V5: NOT RUN (owner runs where gemini is in service; V3/V5 use a candidate copy through the direct CLI invocation in the manifest)",
        "B": "P4 implemented at 8f12bd5: tests/test_investigation_completion.py verifies exact separate B candidate bytes, digest and explicit web denies while preserving 999/998 controls; existing test_review_no_web.py and provider tests retain AGY/render/raw boundaries. triad-b-p4-r1 ADMITTED_SAFE. B1-B3 and A V1-V5 remain NOT RUN; implementation/source equality is not effective-policy proof or revision adoption."
      }
    },
    {
      "id": "C16",
      "surface": "gemini-policy",
      "rule": "R-CONTAIN",
      "summary": "Gemini preflight",
      "input": "gemini 0.33 / a missing capability / an API-key auth path / hardened default with --sandbox omitted",
      "expected": "Refuses below CLI 0.34.0, on a missing capability, when auth is not the approved subscription route; effective posture computed BEFORE the conflict/policy checks (A ordering defect — verified)",
      "tests": {
        "A": "todo",
        "B": "PARTIAL: source 5e8b9fb adds tests/test_gemini_version_floor.py for explicit >=0.34.0 probe, SemVer/failure cases and receipt validation; existing help/capability and competing-auth scrub checks retained. Round triad-b-c16-r1 ADMITTED_SAFE. Runtime auth identity and V1-V5 enforcement are not proven."
      }
    },
    {
      "id": "C17",
      "surface": "gemini-policy",
      "rule": "R-NOCOST",
      "summary": "Credential scrub on the gemini route",
      "input": "a leader env carrying Google credential variables on the gemini route",
      "expected": "Google credential / ADC / Vertex / endpoint / model-selector variables absent from the child env",
      "tests": {
        "A": "todo",
        "B": "Existing coverage: tests/test_provider_wrappers.py::test_gemini_formal_preflight_is_provider_free_and_scrubs_competing_auth and formal dispatch environment test. Scrub does not prove OAuth principal."
      }
    },
    {
      "id": "C18",
      "surface": "gemini-policy",
      "rule": "R-NOCOST",
      "summary": "Google review leg default model",
      "input": "a roster with no gemini model / the Pro-high default / an explicit other model",
      "expected": "Default = the Pro-high tier on agy and on gemini (dispatch-time slug from the roster, never in code); a different model is accepted only as an explicit roster choice to evaluate a future model and is validated against the route's catalog; the resolved model is recorded in the receipt",
      "tests": {
        "A": "todo",
        "B": "GAP / roster dependency: AGY Pro-high/high preflight exists; Gemini formal -m auto is not the agreed Pro-high default."
      }
    },
    {
      "id": "C19",
      "surface": "review-lifecycle",
      "rule": "R-RETRY",
      "summary": "Failed leg, nothing changed",
      "input": "one leg failed to run; source, prompts, roster, model, route unchanged",
      "expected": "Only the failed leg is re-dispatched on the same bound basis; other legs' results are retained; the receipt records attempt 2",
      "tests": {
        "A": "todo",
        "B": "GAP: skills/triad-cross-family-review/references/convergence.md currently requires a new complete round after a failed leg; same-basis attempt reuse is not implemented."
      }
    },
    {
      "id": "C20",
      "surface": "review-lifecycle",
      "rule": "R-REREVIEW",
      "summary": "Reviewed bytes or review conditions changed after a round",
      "input": "a fix wave applied after a round",
      "expected": "New basis digest; every participating leg reviews the full agreed scope; prior findings delivered as fenced data; no approval carried forward; no focused re-confirm path exists",
      "tests": {
        "A": "todo (SKILL.md rule 5(b) deletion at two sites)",
        "B": "PARTIAL: B 7a2fdf3 and tests/test_review_conditions.py bind objective, criteria/order, boundary/order, kind, prepared path, review ID, toolkit bytes and Google receipt/policy bytes into the digest. Existing worktree binding and full fresh-round requirement retained; triad-b-p3b-r2 ADMITTED_SAFE. Prior-finding delivery is separately unproven."
      }
    },
    {
      "id": "C21",
      "surface": "verdict-wire",
      "rule": "R-AGREE",
      "summary": "Blocking finding from an informational leg",
      "input": "an informational leg returning a verified Critical",
      "expected": "Blocks the round until released by an R-AGREE release path; the leg's acceptance label grants no exemption",
      "tests": {
        "A": "todo",
        "B": "PARTIAL / roster dependency: fixed-family severity/open-question tests exist; no arbitrary informational-leg accounting."
      }
    },
    {
      "id": "C22",
      "surface": "roster",
      "rule": "R-ROSTER",
      "summary": "Model and effort reach the actual invocation for every vendor",
      "input": "a roster naming model/effort for each vendor",
      "expected": "The roster's per-vendor block appears in the rendered argv / native tool arguments; an unsupported value is refused at the adapter with the reason",
      "tests": {
        "A": "todo",
        "B": "PARTIAL: formal Claude/AGY argv and native Codex contract present; no common roster-to-invocation matrix; Gemini Auto restriction remains."
      }
    },
    {
      "id": "C23",
      "surface": "agy-containment",
      "rule": "R-BIND, R-CONTAIN",
      "summary": "Two same-family legs in one round",
      "input": "two agy legs in one round",
      "expected": "Each leg has its own bound read-audit evidence and binding; one leg's hook/audit cannot satisfy another's",
      "tests": {
        "A": "todo (read_audit_gate.sh keys on x-* today)",
        "B": "PARTIAL: tests/test_four_leg_custody.py paired receipts; legacy result identifies family/digest, not each same-family leg. Dormant AGY hook stays dormant."
      }
    },
    {
      "id": "C24",
      "surface": "engine-transport",
      "rule": "R-PLATFORM",
      "summary": "Per-platform outcomes",
      "input": "a run on macOS and a run on Ubuntu 24.04",
      "expected": "macOS and Ubuntu 24.04 results recorded separately; an unrun platform is listed NOT RUN",
      "tests": {
        "A": "todo",
        "B": "P4 8f12bd5: dedicated fresh GREEN 108 focused and 1429 full macOS; Ubuntu 24.04 unprivileged uid 65534: 1427 passed, two existing filesystem skips. Skill validator/provider-free lifecycle passed; triad-b-p4-r1 four SAFE, ADMITTED_SAFE, integrity and owned cleanup verified. Not P3-draft, installation or all-provider conformance; current record: decisions/host-b-0555-contract-compliance-handoff.md."
      }
    },
    {
      "id": "C25",
      "surface": "selected-investigations",
      "rule": "R-INVEST",
      "summary": "Selected investigation preserved",
      "input": "one chosen leg with a custom prompt, an authorized extra read root and web enabled",
      "expected": "The chosen model/effort/perspective and the extra root/web reach the actual invocation; the result is free-form or custom-schema; no review verdict is demanded and no roster accounting occurs",
      "tests": {
        "A": "tests/*/dispatch/* (existing --web/--cwd dispatch cases; ids to map)",
        "B": "P4 8f12bd5: tests/test_investigation_completion.py covers authorized raw extra roots, native argv, invalid-root and formal-expansion refusal, Gemini comma-path refusal and custom-schema preservation. Existing raw-route tests remain; both explicit Google web triggers are implemented under C29 without a mandatory investigation envelope."
      }
    },
    {
      "id": "C26",
      "surface": "review-lifecycle",
      "rule": "R-PREPARE",
      "summary": "Symlink in the reviewed basis (owner Q4 ruled)",
      "input": "a dirty basis containing a nonignored symlink to a path outside the tree",
      "expected": "The link itself is reviewed: path, kind and exact link text fingerprinted and visible to legs; the target is never followed automatically; target content enters only as an independently authorized bound input; an unfollowable link is disclosed as a coverage gap; cleanup never follows a link to delete its target; the host's no-follow mechanism leaves evidence",
      "tests": {
        "A": "todo (A refuses untracked links at prepare/verify today)",
        "B": "Guarded adaptation implemented in f08e1e8: scoped HEAD/index/working-tree link records in TASK, all-family no-follow renderer guidance, tests/test_review_link_evidence.py and fresh Terra/high preparation scenario; triad-b-c26-r1 ADMITTED_SAFE. Existing untracked-link fingerprint, prepared-copy refusal and cleanup no-follow checks retained. Reviewer restriction is prompt-controlled, not OS path confinement; target content is not implicitly bound or claimed inspected."
      }
    },
    {
      "id": "C27",
      "surface": "google-resolution",
      "rule": "R-GOOGLE",
      "summary": "Google route resolution",
      "input": "explicit pin / agy installed only / gemini installed only / neither installed",
      "expected": "The resolved route, binary and observed version are frozen for the attempt and recorded; a started leg never switches route; neither-installed = the host's recorded policy (A skip-and-log, B refuse) and is never agreement",
      "tests": {
        "A": "todo",
        "B": "Existing selector/no-fallback and AGY preflight cases plus source 5e8b9fb tests/test_gemini_version_floor.py: exact observed Gemini version in the private receipt and existing receipt-hash binding, invalid/missing versions refused. Round triad-b-c16-r1 ADMITTED_SAFE. This does not adopt future common receipt schemas or claim runtime model identity."
      }
    },
    {
      "id": "C28",
      "surface": "engine-transport",
      "rule": "R-CONTAIN",
      "summary": "Relative --prompt-file / --cwd resolution (recurring dispatch failure)",
      "input": "a wrapper invoked from a cwd with `--prompt-file rel/brief.md` that exists there; the same with a path that does not exist",
      "expected": "Existing relative --prompt-file and --cwd values resolve independently against the wrapper PROCESS cwd captured at entry (never child cwd). Preserve configured runtime roots, regular-file/UTF-8/non-empty/existence checks. Existing success summary and audit represent resolved absolute prompt-file and child-cwd values through the host's existing redaction mode (D-B2); no new log and no unmasked-path expansion. Invalid input exits 3 (argument error) before any vendor call, identifying the resolved candidate through the same masking policy.",
      "tests": {
        "A": "todo (t48-style axis on _common._load_prompt_file; today it refuses at :1167)",
        "B": "Implemented at 8f12bd5 (P4), extending 6653bdc: tests/test_wrapper_relative_paths.py and test_investigation_completion.py cover entry-cwd-relative loading, preserved validation, resolved success/audit fields, masking, refusal and vanished entry cwd. Accepted diagnostic limits are recorded in decisions/host-b-0555-contract-compliance-handoff.md; no unmasked-path expansion."
      }
    },
    {
      "id": "C29",
      "surface": "selected-investigations",
      "rule": "R-INVEST",
      "summary": "Web evidence on a research dispatch is a fetched page, never a search summary",
      "input": "Explicitly web-authorized Google-family INVESTIGATION with caller prompt and optional schema; REVIEW and unrequested raw calls do not trigger clause insertion.",
      "expected": "The vendor prompt ENDS with the shared web-evidence clause after the caller's text; only tool names are substituted per Google CLI. Existing audit/redaction/failure-log/retention rules apply (D-B2); do not require a new permanent exact-prompt or fetched-page store. Verify exact assembly in tests and actual fetch/interpretation using bounded task-authorized evidence. A URL in prose, search summary or successful exit never proves a fetch or complete body. A cited but unfetched/unverifiable URL, placeholder, undated claim or incomplete page evidence remains UNSURE. Preserve existing host read-audit capability without activating dormant hooks for symmetry.",
      "tests": {
        "A": "tests/unit/wrappers/t49-agy-web-evidence-c29.sh (prompt tail + audit record over a fake agy) + the live old-vs-new run recorded in the spike",
        "B": "P4 8f12bd5: test_agy_web_evidence.py plus test_investigation_completion.py verify explicit raw AGY/Gemini triggers, clause-last assembly including Gemini schema repair, caller/custom-schema preservation and refusals. Cross-site live evidence records AGY URL-body prefix loss as non-fatal KI-AGY-URL-BODY-PREFIX under the owner disposition; incomplete evidence stays UNSURE where relevant. No new permanent store, dispatch failure or provider-fix claim. See decisions/2026-09-20-owner-follow-up.md and the current handoff."
      }
    },
    {
      "id": "C30",
      "surface": "verdict-wire",
      "rule": "R-BIND, R-ROSTER",
      "summary": "Canonical v2 schemas reject ambiguous boundaries while preserving agreed result distinctions",
      "input": "v2 verdict with Minor-only negative, uncertainty-only negative, legacy omissions, unsafe/terminal-newline paths or binding; partial roster overrides and shared template; native or CLI transport receipt",
      "expected": "Minor-only negative and uncertainty-only DO NOT MERGE are valid; SAFE with blockers/questions, missing required v2 data, unknown fields, unsafe or newline-terminated paths/identifiers/digests are invalid. Named partial overrides validate before merge; complete resolved entries obey vendor blocks. Native receipts do not invent CLI identity. Schema success is not adapter/binding/admission/runtime proof. A pinned resolved Google route requires its own CLI block; absent/null route preserves host selection and adapter checks.",
      "tests": {
        "shared": "tests/test_schemas.py; original schema absence RED 65 failed; terminal-newline RED 6 failed / 65 passed; final local authoring verification recorded in decisions/rev-2-implementation-spec.md. No host conformance implied.",
        "A": "NOT RUN / host integration pending",
        "B": "Offline candidate validation IMPLEMENTED: bin/validate_v2.py and tests/test_validate_v2.py validate original JSON, canonical shape and six expected bindings with pinned local schemas/no external retrieval. P2 configuration resolver is also implemented. Operational roster/dispatch/render/native-transport/collector integration remains P3; local producer adapter is an uncommitted draft. Adoption remains separate. See current handoff for completed P4 verification."
      }
    }
  ]
}
