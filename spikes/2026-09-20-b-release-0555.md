# B release 0.2.555 and Claude handoff

## Published basis

- B [PR 34](https://github.com/codefoundry-io/triad-codex-dispatch/pull/34)
  merged as `bde3f77301af60c5d13d7fd14f2ed3ed1e8f26dd`.
- Public [v0.2.555 release](https://github.com/codefoundry-io/triad-codex-dispatch/releases/tag/v0.2.555)
  and remote main resolve to that commit. The tested archive was built from
  `a629b65439702283da268b2f425f11a006fd442b`; both commits have the same tree.
- This release packages the completed B fixes recorded in the existing spikes.
  It changes no shared adoption revision, policy contract or pending decision.

## Verification

- Clean macOS archive: 1,201 tests passed; 22/22 source/archive target hashes equal.
- Ubuntu 24.04: 1,199 passed, two existing filesystem skips; source/rootfs read-only,
  network disabled. This is suite evidence, not authenticated cross-platform CLI parity.
- Cumulative round `triad-release-0555-r1`: Claude Opus, Google Pro, Google Flash
  and fresh Codex all SAFE, no open questions; final integrity matched and the
  ledger admitted SAFE. Exact custody export and all three round-owned temporary
  roots were verified before cleanup.
- Supported Codex marketplace/install/bootstrap completed on the owner Mac.
  All 189 tracked source files matched the installed cache; four launchers point
  to 0.2.555. Existing owner config, shell config and classifier hashes were preserved.
- Installed synthetic lifecycle: 14 commands succeeded, JSON payload round-trip
  and ROUND_INTEGRITY_OK, unchanged package hashes, four owned roots absent.
- A fresh read-only Codex process returned `TRIAD_0_2_555_VISIBLE` with exact
  canonical skill SHA-256
  `f0fcbaa467f082248332164e89e16f109aa3732694719510f414ccb6dd906ca2`.
  Two earlier probes dropped a repeated cache-path component while expanding the
  catalog alias; the captured catalog plus deterministic root/suffix expansion
  established a probe error. The successful probe used that exact expansion.
  No source, installation or provider-setting workaround was applied.

## Retained non-blocking observation

Claude identified redundant hashing of recent exported-but-uncleaned roots:
B `bin/review_round.py:983-999` verifies the export and inventory before its age
gate. The leader confirmed the I/O cost and the independent removal checks at
`1312-1336`; it does not alter correctness or deletion eligibility. The optional
ordering change also affects `skipped_roots` reporting and remains a separate
future change requiring verification and fresh review. It is not fixed in 0.2.555.

## A-side action boundary

A remains unchanged at `92c8afd500499d8736afcc28b39a87a4f87fed50`; the compared
review and AGY skill directories are clean. Use the existing per-fix spikes for
exact A paths, lines, preserved guards and proposed changes. No Codex exec leg
should be added to B for symmetry, and no A files were changed by this release.

Read this branch together with `decisions/host-b-finding-closure.md` and the
D-B1/D-B2 proposals. Their five original reviewer-local findings remain open;
rev-2 work remains deferred, shared rev-1 adoption is not claimed, and V1–V5
remain NOT RUN. Publication of this handoff branch is not agreement on those
proposals or authorization to silently change either host's common contract.
